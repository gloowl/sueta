#include <iostream>
//#include <math.h>
using namespace std;
int main() {
    do {
        int N,/* k(0),*/ help(3);
        cout << "Input N: " << endl;
        cin >> N;
        while (help <= N) {
            help *= 3;
            //help = pow(3, k);
            //k++;
        }
        if (help/3 == N) cout << "YES" << endl;
        else cout << "NO" << endl;
    } while (true);
    return 0;
}